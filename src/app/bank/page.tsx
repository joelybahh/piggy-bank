// A nextJS page that simply redirects them back a route as this route should only be coupled with the id as well

export default async function AccountPage() {
  return (
    <div>Account not found</div>
  );
}